# web/src/domains/workflow/

**职责**:编排(workflow)域前端 —— **第四种知识,当前只有一页静态设计预览,没有后端**
(2026-08-27 需求方定:先在导航里占个位置,不做开发)。

设计说明的唯一出处是说明页 `/how-it-works/workflow`(文案在
`src/pages/how-it-works/content.ts` 的 `WORKFLOW` / `WORKFLOW_EXAMPLE`),**本域不重复讲道理**,
只把形状摆出来:左侧节点面板 / 中间画布 / 右侧节点检查器。

| 文件             | 说明                                                                                     |
| ---------------- | ---------------------------------------------------------------------------------------- |
| `module.ts`      | 域描述符(路由 `/ingest/workflow`、识别色 `bg-kb-workflow`);**没有 renderers / citations** —— 没有候选可审、也没有引用可渲染 |
| `IngestPage.tsx` | 域内路由壳(目前只有 index 一页)                                                          |
| `CanvasPage.tsx` | 静态画布预览:横幅说清「不可交互、不落库、不发请求」+ 面板 / 画布 / 检查器三栏,节点顺序与说明页那条客服邮件编排一致 |

**纪律**:一行请求都不发;要改例子里的节点先改说明页,再让这里跟上(两处顺序必须一致)。

详见 `architect.md`;跨域约定见 `../claude.md`。
